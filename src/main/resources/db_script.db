create schema simple_app;

create table test_user
(
    id bigint auto_increment,
    user_name varchar(255) not null,
    tariff varchar(50) not null,
    balance bigint not null,
    constraint test_users_pk
        primary key (id)
);

INSERT test_user(user_name, tariff, balance) VALUES ('6231999531@login', 'SimpleTariff', 22222222);
INSERT test_user(user_name, tariff, balance) VALUES ('1231313131@login', 'TariffPlus', 1313123);
INSERT test_user(user_name, tariff, balance) VALUES ('2312363531@login', 'TariffPlus', 6666666);

create table main_users
(
    id bigint auto_increment,
    email varchar(255) not null,
    first_name varchar(50) not null,
    last_name varchar(100) not null,
    password varchar(255) not null,
    role varchar(20) default 'USER' not null,
    status varchar(20) default 'ACTIVE' null,
    constraint main_users_pk
        primary key (id)
);

create unique index main_users_email_uindex
    on main_users (email);
